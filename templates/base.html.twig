<!DOCTYPE html>
<html>
<head>
    {{ include('partial/_meta-tags.html.twig') }}

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {% if app.environment == 'prod' %}
            <!-- Google tag (gtag.js) -->
            <script async src="https://www.googletagmanager.com/gtag/js?id=G-GZF11L6Q8N"></script>
            <script>
                window.dataLayer = window.dataLayer || [];

                function gtag() {
                    dataLayer.push(arguments);
                }

                gtag('js', new Date());
                gtag('config', 'G-GZF11L6Q8N');
            </script>
        {% endif %}

        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
{{ include('partial/_navbar.html.twig') }}
<div class="min-vh-100">
    {% block body %}{% endblock %}
</div>
</body>
</html>
